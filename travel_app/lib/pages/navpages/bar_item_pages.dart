import 'package:flutter/material.dart';

class BarItemPages extends StatelessWidget {
  const BarItemPages({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "Bar Item Page"
        )
      ),
    );
  }
}